// This is the program for finding the largest element from the array
#include<stdio.h>
void largest();
void largest(){

}

int main(void){
    // taking limit as the input
    int lmt; printf("What's the limit? ");
    scanf("%d",&lmt);
    int arr[lmt];

    printf("Enter the elements one by one ....\n");

    for(int i = 0; i < lmt; i++){
        scanf("%d",&arr[i]);
    }


    // Displaying the elements....
    // for(int ){

    // }
 
    return 0;
}